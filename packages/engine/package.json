{
  "name": "@invoice-extractor/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@invoice-extractor/schema": "workspace:*",
    "unpdf": "^0.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "pdf-lib": "^1.17.1",
    "typescript": "^5.6.3",
    "vitest": "^3.2.6"
  }
}
